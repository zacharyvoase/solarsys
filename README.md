# SolarSystem

Build Solarized-inspired color palettes, using OKHSL and APCA to ensure
perceptual uniformity and accessibility.

## Installation

```bash
$ nvm install && nvm use # use the configured node version
$ npm install
$ npm run dev # run the dev server
```
